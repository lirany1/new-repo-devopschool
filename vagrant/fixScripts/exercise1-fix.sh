#!/bin/bash
#add fix to exercise1 here- #  curl: (7) couldn't connect to host The program 'curl' is currently not installed.  You can install it by typing:sudo apt-get install curl
#we are using apt-get install with all prompts and run non-interactively.
sudo apt-get -y install curl
